\section{问题四模型的建立与求解}

\subsection{问题四的描述与分析}

问题二只调整任务的目标区域与开工时间而保持附件能源动作不变，问题三则固定计算负荷只优化储能；问题四要求\textbf{同时优化任务排程与能源--储能系统}。附件中 RT 的服务边界严格，而 AI Training 贡献约 80.14\% GPU-hour 和 87.12\% AI IT 能量；区域的网络、算力、PUE、储能和售电优势又彼此错位。更关键的是，迁入任务与储能充电会竞争同一新能源余量，因此问题二的固定能源边际并不等于储能重优化后的真实边际。

因此将任务放置直接映射为 AI IT 与设施负荷，并对每个候选负荷状态重新优化能源和储能。约 $2.33\times10^8$ 个合法任务放置由区域多割 Benders 与完整域列生成联合处理；服务质量则在成本最优面内依次压缩总等待和网络时延。工作负载转移与能源系统需要联合管理的思想与近期数据中心 integrated workload--energy management 研究一致\cite{guo_integrated_2025}。总体流程见图\ref{fig:q4-flow}。

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{\ProjectRoot/modules/50_q4/figures/q4_flow.png}
\caption{问题四计算--能源联合优化总体流程}
\label{fig:q4-flow}
\end{figure}

题面要求同时考察 Cost、Carbon、Latency、QoS、RenewableUtilization 与 regional PeakNetImport。本文不把六项指标等权压缩为单一分数：基准场景以 Cost 为第一经济目标，在其最优面内依次压缩总等待和网络时延；Carbon 在碳预算场景中作为 $\varepsilon$-constraint，新能源利用率与区域峰值净购电则保持题面统一口径作为系统评价量。这样既保留各指标的物理含义，也避免在指标退化或高度同向时人为制造权重冲突。

\subsection{预备工作：联合优化必要性与顺序失真}

50000 个任务数量近似三等分，但资源权重和可调节范围差异明显：Training、Batch、RT 分别约占 80.14\%、15.32\%、4.54\% GPU-hour；RT 到达即开工且合法区域最少。RegionD 的 AvailableGPU 最大（1472），D/E/F 的 PUE 更低且储能、售电能力更强，而 A/B/C 网络时延更有利。六区域逐时 $\mathrm{AvailableRenewable}$ 又完全相同，所以区域绿色优势不能由标签预设，只能由 PUE、负荷、价格、碳强度、储能和售电边界共同决定。

给定任务排程 $\mathbf x$，动态新能源盈余为
\begin{equation}
H_{rt}(\mathbf x)=\mathrm{AvailableRenewable}_{rt}-L_{rt}^{Fac}(\mathbf x).
\end{equation}
迁入任务可以当期消纳新能源，储能也可以利用同一余量充电并跨时释放，因此二者对新能源余量存在竞争，同时又能在不同时间尺度上相互补充。

顺序优化探针进一步验证了这一点：选取 3 个仅能在 E/F 执行的真实 RT 任务，共 8 种放置。固定能源动作时，“全部迁 F”可节约约 3613.67 CNY；但分别重求完整能源--储能 LP 后，8 种放置的最优能源成本相同，此时保留在 E 的方案网络时延更低。因此
\begin{equation}
\text{固定能源边际价值}\neq\text{储能重优化后的真实边际价值}.
\label{eq:q4-sequential-failure}
\end{equation}
能源 LP 的负荷平衡对偶与真实边际成本排序仍高度相关（Pearson 约 0.985、Spearman 约 0.898），适合形成累积 Benders 支撑超平面，而不适合只保留单次线性近似。

\subsection{IWESS 计算--能源--储能联合调度模型}

本文将上述联合模型记为 \textbf{Integrated Workload--Energy--Storage Scheduling（IWESS）}。该名称用于概括任务时空决策与 BESS/电网连续再优化构成的联合数学对象，不把缩写本身作为创新声明。对任务 $i$ 按 SLA、Arrival、LatestFinish 与 2406 边界构造完整合法放置集 $\Omega_i$，仅对 $(r,s)\in\Omega_i$ 定义 $x_{irs}\in\{0,1\}$。任务与小时的重叠量为
\begin{equation}
\omega_{ist}=\max\bigl(0,\min(t+1,s+p_i)-\max(t,s)\bigr),
\end{equation}
从而
\begin{align}
L^{AI}_{rt}(\mathbf x)&=\sum_{i,s}\alpha_{k_i}g_i\omega_{ist}x_{irs},\label{eq:q4-ai-load}\\
L^{Fac}_{rt}(\mathbf x)&=\mathrm{PUE}_r\bigl(L^{NonAI}_{rt}+L^{AI}_{rt}(\mathbf x)\bigr).\label{eq:q4-fac-load}
\end{align}

能源侧沿用问题三 BESS-ED 变量 $\mathbf y_r=(u,qR,qG,d,gL,s,w,E)$。对给定设施负荷 $L_r^{Fac}$，区域可行集 $\mathcal Y_r(L_r^{Fac})$ 由新能源平衡、负荷平衡、SOC 递推、充放电功率、购售电和终端 SOC 约束共同定义。联合目标为
\begin{align}
\mathrm{Cost}(\mathbf y)&=\sum_{r,t}\bigl(c^{buy}_{rt}G^{buy}_{rt}-c^{sell}_{rt}s_{rt}\bigr),\label{eq:q4-cost}\\
W(\mathbf x)&=\sum_{i,r,s}(s-a_i)x_{irs},\qquad
D(\mathbf x)=\sum_{i,r,s}\ell_{o_i r}x_{irs}.\label{eq:q4-qos}
\end{align}

\subsubsection{联合模型汇总}

IWESS 的基准字典序目标直接展开为
\begin{equation}
\operatorname*{lex\,min}_{\mathbf x,\mathbf y}\quad
\left(
\sum_{r,t}\bigl(c^{buy}_{rt}G^{buy}_{rt}-c^{sell}_{rt}s_{rt}\bigr),
\quad
\sum_{i,r,s}(s-a_i)x_{irs},
\quad
\sum_{i,r,s}\ell_{o_i r}x_{irs}
\right)
\label{eq:q4-summary-obj}
\end{equation}
\begin{equation}
\mathrm{s.t.}\quad
\left\{
\begin{aligned}
&\sum_{(r,s)\in\Omega_i}x_{irs}=1,&&\forall i,\\
&\sum_{i,s}g_i\omega_{ist}x_{irs}\le\mathrm{AvailableGPU}_r,&&\forall r,t,\\
&L^{IT}_{rt}(\mathbf x)\le\mathrm{MaxITPower}_r,\quad
L^{Fac}_{rt}(\mathbf x)\le\mathrm{MaxFacilityPower}_r,&&\forall r,t,\\
&\mathbf y_r\in\mathcal Y_r\bigl(L_r^{Fac}(\mathbf x)\bigr),&&\forall r,\\
&x_{irs}\in\{0,1\},&&(r,s)\in\Omega_i.
\end{aligned}
\right.
\label{eq:q4-summary-con}
\end{equation}
其中 $\Omega_i$ 已编码网络 SLA、Arrival、LatestFinish、RT 即时开工和 finish$\le2406$；$\mathcal Y_r$ 内含问题三全部能源、储能与电网物理约束。

\subsection{模型求解：Benders--CG 与字典序精化}

给定任务负荷后，区域最小能源成本为
\begin{equation}
Q_r(L_r)=\min_{\mathbf y_r\in\mathcal Y_r(L_r)}\sum_t\left(c^{buy}_{rt}G^{buy}_{rt}-c^{sell}_{rt}s_{rt}\right),
\end{equation}
基准成本场景无全局碳预算，故 $Q(L)=\sum_rQ_r(L_r)$。设第 $k$ 个负荷点的负荷平衡对偶为 $\lambda_{rt}^k$，区域最优性割为
\begin{equation}
\theta_r\ge Q_r(L_r^k)+\sum_t\lambda_{rt}^k\left(L_{rt}^{Fac}(\mathbf x)-L_{rt}^{Fac}(\mathbf x^k)\right).
\label{eq:q4-benders-cut}
\end{equation}

完整合法域约含 $2.33\times10^8$ 个放置，故只保留活动列并对完整隐式域做精确定价，不使用 Top-k 或人工等待窗。Benders decomposition 与 column generation 的组合是处理大规模离散调度与分解结构的成熟求解路线，近期 Omega 研究亦给出了二者联合用于大规模 allocation/scheduling 的实例\cite{wang_bcg_2026}。附件满足 $\mathrm{MaxFacility}=\mathrm{PUE}\times\mathrm{MaxIT}$ 且 $\mathrm{FacilityLoad}=\mathrm{PUE}\times\mathrm{ITLoad}$，因此实现中无损删除重复 Facility 行；Benders 割按非零对偶稀疏存储。40-task 显式校准含 3139 个合法放置，列生成仅用 44 个活动列即可得到相同根节点目标值。

字典序求解分三层进行。第一层最小化 Cost；第二层在 $\mathrm{Cost}\le C^*+\varepsilon_C$ 内最小化总等待；第三层固定前两层锚点后最小化总网络时延。每一层均重新执行完整域定价和真实能源复核；若后序阶段产生新列，则在最终扩展列池上从第一层重新认证。算法见算法\ref{alg:q4-benders-cg}。

\input{\ProjectRoot/modules/50_q4/paper/q4_algorithm.tex}

\subsection{结果与分析}

40-task 精确校准中，仅最小成本可出现 38/40 个任务迁移、总等待 172 h；加入等待与时延目标后得到迁移 5 个、总等待 6 h、最大等待 2 h、总时延 437 ms 的精确字典序方案，说明服务质量精化确有必要。

50k 最终扩展列池包含 137158 个活动列和 1173 条 Benders 割，两轮再认证结果完全一致。

\begingroup  
\rowcolors{2}{gray!10}{white}   
\begin{table}[!htbp]
\centering
\caption{40-task Exact 校准结果}
\label{tab:q4-40task}
\begin{tabular}{lcc}
\toprule
\rowcolor{CumcmLightBlue}
指标 & 显式展开 & 列生成 \\
\midrule
Legal placements & 3,139 & 44（active）\\
Root objective/CNY & $-5{,}450{,}201.934546$ & $-5{,}450{,}201.934546$ \\
绝对差/CNY & --- & 0.0 \\
Benders 迭代 & --- & 2 \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

在同一 40-task 完整合法域上继续做字典序精化：Cost-only 的某些等价代表解可出现 38/40 迁移、总等待 172 h；而在 Cost 最优面内依次最小化 Wait、Latency 后，可得到迁移 5 个任务、总等待 6 h、平均等待 0.15 h、最大等待 2 h、总网络时延 437 ms、平均时延 10.925 ms 的 exact 字典序代表解。该结果说明“Cost 最优值接近”不能替代服务质量精化，也验证了 Wait/Latency 必须重新做 full-domain pricing。

\subsubsection{50k final-pool 字典序再认证结果}

50,000 个任务的最终扩展列池包含 137,158 个活动列、1,173 条 region Benders cuts。两轮完整 Cost$\rightarrow$Wait$\rightarrow$Latency 再认证得到完全一致的三个 anchor：

\begin{table}[!htbp]
\centering
\caption{50k final-pool 字典序再认证结果}
\label{tab:q4-final-recert}
\begin{tabular}{lccc}
\toprule
\rowcolor{CumcmLightBlue}
指标 & 第1轮 & 第2轮 & 最终代表值 \\
\midrule
真实能源成本/CNY & $-459{,}340{,}688.8007$ & $-459{,}340{,}688.8007$ & $-459{,}340{,}688.8007$ \\
总等待/h & 0 & 0 & 0 \\
总网络时延/ms & 250,860 & 250,860 & 250,860 \\
活动列数 & 137,158 & 137,158 & 137,158 \\
Benders 割数 & 1,160 & 1,173 & 1,173 \\
最小缺失列约化成本 & 0 & 0 & 0 \\
最大区域 Benders 违反/CNY & 0 & 0 & 0 \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

最终排程覆盖全部 50000 个 TaskID，等待均为 0；仅 61 个任务跨区，迁移率 0.122\%，其中 Training 52、Batch 8、RT 1。总网络时延 250860 ms，平均 5.0172 ms，P95 为 5 ms，最大 58 ms，最晚完成 2405.6 h；任务、容量、SLA、截止时间、能源平衡、SOC 和购售电边界全部通过审计，最大物理残差约 $1.14\times10^{-13}$。

\noindent\textbf{（1）联合调度对任务迁移与服务质量的改善}\quad
问题二只调整任务排程时，成本优先方案需迁移 74.896\% 的任务，平均等待 20.475 h、最大等待 2016 h；问题四允许任务排程与储能、电网同时优化后，总等待降为 0，迁移率仅 0.122\%。因此，在同一任务与能源数据下，\textbf{联合优化避免了单纯依靠大规模跨区迁移和长时间延迟来改善能源成本，在维持联合模型成本最优面的同时显著改善了服务质量}。

问题三与问题四当前绝对成本差约 $1.23\times10^5$ CNY，但第 2406 h 跨问结算口径尚待统一，因此暂不把该差额解释为正式联合增量收益。

\subsection{模型检验与六指标场景分析}

最终两轮均无缺失负约化成本列，区域 Benders 违反为 0，最终活动列池 MIP gap 为 0；但这不能自动推出完整域整数全局最优。时延层完整域 LP 下界为 250469.9546 ms，最终整数值 250860 ms，相对差
\begin{equation}
\frac{250860-250469.9546}{250860}\approx0.1555\%.
\end{equation}
因此正式表述为：\textbf{完整域 LP 定价与区域 Benders 已闭合，并获得真实能源可行、两轮再认证稳定的最优已知字典序整数方案；不声称完整域整数全局最优。}

三类场景均沿用同一 IWESS 数学模型，仅改变碳预算、电价序列或新能源供给。对于已经完成 Cost、Wait、Latency 再认证或与基准场景数学等价的方案，统一比较运行成本、碳排放、网络时延、服务质量、新能源利用率和区域峰值净购电；新能源下降 20\% 场景因 Cost 层尚未闭合，单独报告严格上下界和可行候选，避免把不同证据等级混为一谈。

\noindent\textbf{（1）基准方案的六指标权衡}\par
基准联合方案的净运行成本为 $-4.5934\times10^8$ CNY，碳排放为 0，总等待为 0，平均网络时延为 5.0172 ms，新能源利用率为 69.52\%，六区域正向峰值净购电均为 0。由此可见，在附件给定的新能源、储能和售电条件下，经济性、低碳性与服务质量并未形成明显冲突：模型首先通过新能源、储能和区域购售电协调吸收能源侧波动，仅需 0.122\% 的任务跨区即可同时维持零等待和较低网络时延。表\ref{tab:q4-formal-certified}进一步给出不同外生场景下的统一六指标比较。

\begin{table}[!htbp]
\centering
\caption{Q4 已认证或与基准等价的正式场景六指标比较}
\label{tab:q4-formal-certified}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrrrrr}
\toprule
\rowcolor{CumcmLightBlue}
类别 & 场景 & Cost/$10^6$ CNY & Carbon/tCO$_2$ & Wait/h & 平均时延/ms & 新能源利用率/\% & 峰值净购电/MW & 迁移率/\% \\
\midrule
碳预算 & 100\% & -459.340689 & 0 & 0 & 5.0172 & 69.5208 & 0 & 0.122 \\
碳预算 & 75\%  & -459.340689 & 0 & 0 & 5.0172 & 69.5208 & 0 & 0.122 \\
碳预算 & 50\%  & -459.340689 & 0 & 0 & 5.0172 & 69.5208 & 0 & 0.122 \\
碳预算 & 25\%  & -459.340689 & 0 & 0 & 5.0172 & 69.5208 & 0 & 0.122 \\
\midrule
电价 & 附件逐时价格 & -459.340689 & 0 & 0 & 5.0172 & 69.5208 & 0 & 0.122 \\
电价 & 区域平坦价格 & -459.340689 & 0 & 0 & 5.0172 & 69.5209 & 0 & 0.122 \\
电价 & 峰谷强化价格 & -459.340716 & 0.8616 & 0 & 5.0172 & 69.5208 & 0 & 0.122 \\
\midrule
新能源 & 名义供给 & -459.340689 & 0 & 0 & 5.0172 & 69.5208 & 0 & 0.122 \\
新能源 & 上升 20\% & -459.341389 & 0 & 0 & 5.0084 & 57.7750 & 0 & 0.066 \\
\bottomrule
\end{tabular}}
\end{table}
\FloatBarrier

Benders cut 的 dual support 平均仅 3.4--4.1 个非零元素，平均 cut 密度约 2.6--3.4\%。稀疏装配后 500-task 的 cut 存储从 9.17 MB 降至 0.38 MB（下降约 95.8\%）。这些压缩均来自附件数据中的 exact dominance 或实际 dual support，而不是牺牲可行域换取速度。

\subsection{模型检验与场景边界}

\subsubsection{诊断性碳约束敏感性}

在 $\gamma=1.4$ 新能源波动压力探针（临界放大因子 $\gamma_{\text{crit}}\approx1.303326$）下，沿 Cost--Carbon epsilon-constraint 路径得到表\ref{tab:q4-carbon}。碳预算从 100\% 降至 0\% 时，成本增加约 0.1\%（约 4961.32 CNY），局部平均减排成本约 92.48 CNY/tCO$_2$。该探针仅用于验证 Carbon 约束被激活时模型能够产生可解释权衡，\textbf{不构成题目正式新能源波动场景}。

\begin{table}[!htbp]
\centering
\caption{新能源下降 20\% 场景的严格 Cost 区间与可行候选指标}
\label{tab:q4-renew-down-bounded}
\begin{tabular}{lc}
\toprule
\rowcolor{CumcmLightBlue}
指标 & 数值 \\
\midrule
完整域 Cost 下界/$10^6$ CNY & -491.584160 \\
整数可行 Cost 上界/$10^6$ CNY & -401.445986 \\
Cost 相对 Gap & 22.45\% \\
候选 Carbon/tCO$_2$ & 2698.997 \\
候选总等待/h & 389,579 \\
候选平均等待/h & 7.7916 \\
候选平均网络时延/ms & 29.4058 \\
候选新能源利用率/\% & 84.1274 \\
候选峰值净购电/MW & 115.6045 \\
候选迁移率/\% & 74.504 \\
硬约束审计 & 全部通过 \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

\subsubsection{50k 可扩展性与最优性边界}

Cost 初始 root 阶段 50k 任务的实际峰值内存约 0.254 GiB，总求解时间约 2.06 min；后续完整字典序再认证将活动列扩展至 137,158，并最终形成 1,173 条 region cuts。两轮 final-pool sweep 中所有阶段的完整域定价均无负 reduced-cost 缺失列，最大区域 Benders violation 为 0，且 restricted-column MIP gap 为 0。

需要强调，`restricted MIP gap=0` 只证明\textbf{当前最终活动列池上的整数问题}已经解到最优，不等于完整域全局整数最优证书。Latency 阶段的完整域 LP 下界为 250,469.9546 ms，而最终整数代表解为 250,860 ms，二者绝对差约 390.0454 ms，相对差约
\begin{equation}
\frac{250860-250469.9546}{250860}\approx 0.1555\%.
\label{eq:q4-lat-gap}
\end{equation}
因此本文的正式口径是：\textbf{完整合法域 LP pricing 与 region Benders 已闭合，并在最终扩展列池上获得真实能源可行、两轮再认证稳定的 best-known 字典序整数代表解；不声称 50k 全局整数最优。}

\subsubsection{六指标的角色说明}

题面要求报告 Cost、Carbon、Latency、QoS、RenewableUtilization、regional PeakNetImport 六项指标。根据当前 canonical 结果与数据探针，它们在模型中的角色为：

\begin{itemize}
  \item \textbf{Cost}：第一字典序优化层；
  \item \textbf{Carbon}：在数据或场景真正激活冲突时通过 epsilon-constraint 纳入，不预设 Cost/Carbon 权重；
  \item \textbf{QoS/Wait}：Cost 面上的第二层字典序精化，50k canonical 已达到 Total Wait=0；
  \item \textbf{Latency}：Wait 后的第三层字典序精化，50k canonical 总时延 250,860 ms；
  \item \textbf{RenewableUtilization}、\textbf{regional PeakNetImport}：作为题面要求的正式报告指标，在 canonical 与后续场景中统一核算；是否成为独立优化轴仍由冲突探针决定，而不是因为题面列出六项就重复计权。
\end{itemize}

因此，本问不使用六指标等权综合目标、AHP、熵权或人为 minimax 权重。当前 canonical 联合优化主问题已经完成，但 Q4 整问尚需在相同 joint model 下完成三类正式场景：\textbf{Carbon constraints、electricity-price mechanisms、renewable fluctuation scenarios}。每个场景均必须重新允许 workload placement 与 BESS/Grid 联合优化并重新执行 exact pricing；历史 $\gamma=1.4$ 仅作为诊断探针，不替代正式场景。
