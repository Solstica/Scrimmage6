# Q4 完整代码与运行记录归档

归档时间：2026-08-19

本目录用于队友审核和复现实验，集中保存当前 Q4 分支的代码、模型说明、运行报告、场景结果、指标、日志、断点和绘图资料。

## 目录

- `code/`：当前 Q4 Python 源码、测试、代码审计和算法诊断文档。
- `records/`：`modules/50_q4/results/` 中的 JSON、CSV、日志、错误日志、Markdown 报告和 NPZ 断点，按原结果目录结构保存。
- `tasks/`：Q4 实时任务与最终化任务记录。
- `paper/`：当前 Q4 正文源文件。
- `figures/`：Q4 图形、绘图数据和绘图报告。
- `MANIFEST_SHA256.csv`：归档文件的 SHA-256 清单。

## 重要状态

- canonical Q4：`FINISHED_DRAFT`，最终 Cost -> Wait -> Latency 再认证和硬约束审计已通过。
- `renew_down_80`：保留完整历史运行、断点和可行候选；全规模正式根节点尚未闭合，不得写成全局整数最优。
- `q4_qcert.py`：快速有界证书脚本已归档，但本次归档时尚未作为完整 Q4 场景结论运行。

## 归档边界

场景附件、重复备份目录和 pytest 临时目录没有重复复制；原始输入仍保留在各自场景目录中。归档不改变、不覆盖、不删除任何原始结果。
