本文围绕题目给出的对象、数据与约束，建立从机理分析、数学建模到数值求解和结果检验的完整方法。开赛后将本段替换为全文任务概述，控制在约2--3行，并只概括最终采用的模型主线，不提前堆叠细节。

\textbf{针对问题一，}先完成必要的数据处理或机理分析，再建立\textbf{核心模型名称}，明确输入、输出、目标函数或主要关系，并采用相应算法求解。最终给出\textbf{关键结果示例}，随后用一句话解释结果所反映的规律或实际含义。正式写作时建议按“任务--准备--模型--算法--结果--解释”的功能顺序组织，再压缩为约4--6行。

\textbf{本文特点：}一是各问模型之间保持清晰的递进关系，避免相互独立地堆叠方法；二是将关键结论同时落实到数值结果、图表和必要的稳健性或误差分析中。

\keywords{关键词1；关键词2；关键词3；关键词4}
