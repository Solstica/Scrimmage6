# Q2 绘图报告

本版按 `modules/30_q2/paper/FIGURE_REVIEW_20260817.md` 和数学建模图表规范修订。16 份候选数据表保留在目录中，正文保留 6 组主图；其余图作为附录或备选。所有 CSV 均为 UTF-8 with BOM；类别轴使用 `类别_名称`，连续变量使用 `X_名称（单位）` 或 `Y_名称（单位）`，标签列使用 `标签_名称`，可直接导入 Origin 中文版。

## 一、正文主图

| 主图组 | 组成 | 主要结论 | 推荐图型 |
|---|---|---|---|
| Q2-1 任务柔性三面板 | 图01、图02、图03 | 空间合法区域、实际可调开工时长和 GPU-hour 共同解释 SFETA 排序；RT 的实际开始时刻自由度为 0 | 三面板：区间点图、水平棒棒糖、棒棒糖 |
| Q2-2 基准能源时空错配及低碳属性 | 图13a 两表、图13b | 91.9% region-hour 同时弃电与购电，且累计弃电约为 AI 设施侧能量的 8.15 倍；弃电高的时段通常更低碳 | 共存占比堆积条 + 能量量级条形 inset + 相关系数组柱图 |
| Q2-3 分段能源边际响应 | 图11 | 负荷扰动在弃电吸收、购电边际和降负荷回退区呈不同机制 | 分类散点图 |
| Q2-4 收敛与初始化敏感性 | 图04、图05 | 重复完整扫描收敛，previous-start 终点更差，且 Cost/Carbon 轨迹总体同向 | 双面板折线图 |
| Q2-5 区域迁移热图 | 图09 | 由真实矩阵识别来源区到执行区的迁移集中，不预先指定承接中心 | 6×6 热图 |
| Q2-6 Cost–Carbon 端点关系 | 图12 | Baseline、Cost-primary、Carbon-primary、previous-start 的成本—碳排位置清晰；不称为 Pareto 前沿 | 带标签散点图 |

## 二、正文主图操作说明

### Q2-1(a) 空间合法区域数

- 数据表：`图01_任务空间柔性范围.csv`
- 选列：`类别_任务类型`、`Y_空间合法区域数下限（个）`、`Y_空间合法区域数上限（个）`
- Origin：导入 CSV 后选择“绘图”→“柱形/条形/饼图”→“浮动柱形图”；若版本没有浮动柱形，使用区间柱形图。
- 设置：Y 轴标题“空间合法区域数（个）”，范围 0–6.5；下限浅蓝、上限深蓝；显示整数标签；图例为“下限”“上限”。
- 结论：AI 训练始终有 6 个合法区域，批量推理为 5–6 个，实时推理为 1–3 个，空间迁移柔性逐渐降低。

### Q2-1(b) 实际可调开工时长中位数

- 数据表：`图02_截止时间余量中位数.csv`
- 选列：`类别_任务类型`、`Y_实际可调开工时长中位数（h）`
- Origin：选择“绘图”→“统计图”→“棒棒糖图”；没有该菜单时选择“基本二维图”→“散点图”，再添加连接至零基线的垂线。
- 设置：Y 轴标题“实际可调开工时长中位数（h）”，范围 0–1300；显示 2 位小数。实时推理以深红实心点标记并标注 `0 h`，不得以零高度柱替代。为兼容已创建的 OPJU，本 CSV 文件名暂沿用旧名，但列名和数值已按实际决策自由度修正。
- 结论：实时推理受 `StartHour=ArrivalHour` 硬约束，实际开始时刻自由度严格为 0；批量推理和训练任务才承担时间迁移空间。

### Q2-1(c) GPU-hour 中位数

- 数据表：`图03_任务GPU小时中位数.csv`
- 选列：`类别_任务类型`、`Y_GPU小时中位数（GPU·h）`
- Origin：选择“绘图”→“统计图”→“棒棒糖图”；没有该菜单时使用散点图并添加垂线基线。
- 设置：Y 轴标题“GPU小时中位数（GPU·h）”；AI 训练橙色、批量推理蓝色、实时推理灰色；显示 2 位数据标签；Y 轴从 0 起。
- 结论：AI 训练单任务 GPU-hour 中位数约 194.35，资源重量显著高于另外两类。

### Q2-2(a) 基准弃电—购电并存及能量量级

- 数据表：`图13a_弃电购电共存统计.csv`、`图13a_弃电与AI设施侧能量.csv`
- 绘图工具：Origin。
- 图形任务：同时量化“弃电与供负荷购电并存”的频率及其能量量级，说明问题的核心是时空错配而非新能源总量不足。
- 主图操作：导入第一张表，选中 `类别_统计范围`、`Y_同时弃电且购电区域时数（个）`、`Y_其他区域时数（个）`，选择“绘图”→“柱形/条形/饼图”→“百分比堆积条形图”。将“同时弃电且购电”设为深蓝绿色，“其他区域时数”设为浅灰；只保留一根横向条，在深蓝绿色段内标注 `13 230（91.9%）`，在图注中写明总数为 14 400 个区域·小时。
- 能量量级 inset：另导入第二张表，选中 `类别_能量指标`、`Y_累计能量（MWh）`，选择“绘图”→“柱形/条形/饼图”→“水平条形图”。条形末端显示科学计数法数值；弃电用橙色、AI 设施侧能量用深灰。在该小图右上角添加文本 `7.748×10^6 / 0.951×10^6 = 8.15`。将此小图作为主图右上角 inset，或用“图形”→“合并图窗”与主图横向并列。
- 坐标与版式：主图不设多余纵轴标题；inset 横轴标题“累计能量（MWh）”。两图共用总图题“基准能源时空错配”。
- 结论：14 400 个区域·小时中，13 230 个同时存在弃电和供负荷购电，比例为 91.9%；累计弃电量约为基准 AI 设施侧能量的 8.15 倍。因此调度价值来自匹配负荷与既有弃电的时空位置。

### Q2-2(b) 弃电的低碳属性

- 数据表：`图13b_弃电低碳属性相关性.csv`
- 绘图工具：Origin。
- 图形任务：比较六区域中弃电与碳强度、可用新能源的 Pearson 相关性，验证优先吸收弃电具有数据驱动的低碳属性。
- 选列：`类别_区域`、`Y_弃电与碳强度Pearson相关系数`、`Y_弃电与可用新能源Pearson相关系数`。
- Origin：选择“绘图”→“柱形/条形/饼图”→“分组柱形图”。
- 设置：Y 轴标题“Pearson 相关系数”，范围固定为 -1 至 1，并在 0 处添加浅灰参考线；弃电—碳强度为蓝色，弃电—可用新能源为绿色。显示 3 位小数，图例仅保留“与碳强度”“与可用新能源”。
- 结论：六区弃电与碳强度均为显著负相关（-0.690 至 -0.849），与可用新能源均为强正相关（0.925 至 0.995）。这是相关关系而非因果识别，但足以说明“优先吸收弃电”不需要另设主观新能源—碳权重。

### Q2-4(a) 成本收敛与初始化敏感性

- 数据表：`图04_成本收敛轨迹.csv`
- 选列：`X_扫描轮次（次）`、`Y_参考起点运行成本（CNY）`、`Y_前次起点运行成本（CNY）`
- Origin：选择“绘图”→“基本二维图”→“折线图”。
- 设置：X 轴“完整扫描轮次”，Y 轴“运行成本（CNY）”；reference-start 深蓝实线圆点，previous-start 橙色虚线方点；图例保留英文起点名称；第 3、4 遍标注端点值。
- 结论：reference-start 第 3 遍达到约 1.555×10^9 CNY 并收敛；previous-start 第 4 遍仍收敛到更高成本，说明存在初始化路径依赖。

### Q2-4(b) 碳排收敛

- 数据表：`图05_碳排收敛轨迹.csv`
- 选列：`X_扫描轮次（次）`、`Y_Cost主端点碳排放（tCO₂）`、`Y_Carbon主端点碳排放（tCO₂）`
- Origin：选择“绘图”→“基本二维图”→“折线图”，作为 Q2-2 的第二图层或第二面板。
- 设置：Y 轴“碳排放（tCO₂）”；Cost 主端点深蓝，Carbon 主端点绿色；第 3 遍标注端点值；不要添加拟合线。
- 结论：Carbon-primary 的碳排端点低于 Cost-primary，但两条收敛轨迹总体同向，不构造人工加权目标。

### Q2-3 分段能源边际响应

- 数据表：`图11_负荷增量与能源响应.csv`
- 选列：`X_设施负荷扰动ΔL（MW）`、`Y_弃电功率变化ΔC（MW）`、`Y_电网购电功率变化ΔG（MW）`、`类别_能源响应分段`
- Origin：选择“绘图”→“基本二维图”→“散点图”，将两列 Y 作为两个系列；使用 `类别_能源响应分段` 做点颜色分组，不筛选、不删除数据。
- 设置：X 轴“任务负荷增量 ΔL（MW）”；Y 轴“能源响应变化（MW）”；弃电变化用橙色圆点，购电变化用蓝色方点；按分段类别使用不同点色；添加 `ΔL=0` 和 `ΔGridPurchase=0` 参考线；不加拟合线。
- 图中分段标注：
  - `0 ≤ ΔL ≤ C0`：弃电吸收区，`ΔGridPurchase=0`；
  - `ΔL > C0`：购电边际区，原弃电耗尽后购电增加；
  - `ΔL < 0` 且购电仍为正：降负荷减购电区；
  - 购电降为 0 后继续降负荷：降负荷回退弃电区。
- 结论：能源响应是分段的，不能概括为“负荷下降时弃电和购电同步下降”。

### Q2-5 SourceRegion 到 ExecutionRegion 迁移热图

- 数据表：`图09_区域迁移矩阵.csv`
- 选列：`类别_来源区域`、`Y_区域A执行任务数（个）`、`Y_区域B执行任务数（个）`、`Y_区域C执行任务数（个）`、`Y_区域D执行任务数（个）`、`Y_区域E执行任务数（个）`、`Y_区域F执行任务数（个）`。
- Origin：导入后选择“绘图”→“等高线/热图”→“热图”；若需要，先用“工作表”→“转换为矩阵”。
- 设置：横轴执行区域，纵轴来源区域；色标标题“迁移任务数（个）”；使用连续色带并显示整数单元格标签。
- 结论：正文只根据热图中实际高值单元描述迁移集中方向，不预先写死“区域 C/E 是承接中心”。

### Q2-6 Cost–Carbon 端点散点

- 数据表：`图12_成本碳排端点散点.csv`
- 选列：`X_运行成本（CNY）`、`Y_碳排放（tCO₂）`、`标签_方案`
- Origin：选择“绘图”→“基本二维图”→“散点图”；将 `标签_方案` 设置为数据标签。
- 设置：X 轴“运行成本（CNY）”，Y 轴“碳排放（tCO₂）”；附件基准灰色叉号，Cost-primary 深蓝圆点，Carbon-primary 绿色菱形，previous-start 橙色方点；不连接散点、不加拟合线。
- 图注：Carbon-primary 相对 Cost-primary 的成本代价约 0.1789%，Cost-primary 相对 Carbon-primary 的碳代价约 0.6193%。不得使用“Pareto frontier”表述。
- 结论：两个 canonical 端点高度同向但不完全重合；previous-start 同时偏向更高成本和更高碳排。

## 三、附录候选图

### 附录 A：方案运行成本柱图

- 数据表：`图06_方案运行成本对比.csv`
- 选列：`类别_方案`、`Y_运行成本（CNY）`
- 图型：Origin“柱形图”；Y 轴“运行成本（CNY）”。
- 用途：只作为端点散点图的数值补充，不作为正文主图。

### 附录 B：方案碳排柱图

- 数据表：`图07_方案碳排放对比.csv`
- 选列：`类别_方案`、`Y_碳排放（tCO₂）`
- 图型：Origin“柱形图”；Y 轴“碳排放（tCO₂）”。
- 用途：只作为端点散点图的数值补充，不与 Q2-5 同时占用正文版面。

### 附录 C：Cost-primary 逐时能源状态

- 数据表：`图10_逐时能源状态.csv`
- 选列：`X_时间（h）`、`Y_设施负荷（MW）`、`Y_电网购电功率（MW）`、`Y_弃电功率（MW）`、`Y_新能源利用功率（MW）`
- 图型：Origin“折线图”；X 轴 0–2405 h，Y 轴“功率（MW）”。
- 用途：只有在实际 Origin 画面中四条曲线仍清晰可读时才进入正文，否则放附录。

### 附录 D：任务类型 Mean/P95 等待

- 数据表：`图08_任务类型等待分布.csv`
- 选列：`类别_任务类型`、`Y_平均等待时间（h）`、`Y_等待时间95%分位数（h）`
- 图型：Origin“分组柱状图”；Y 轴“等待时间（h）”。
- 用途：正文只画 Mean 与 P95；最大等待 2016 h 不再塞入同一线性坐标图，具体合法性由 `../results/q2_extreme_wait_audit.csv` 审计表支撑。

### 附录 E：代表性任务调度甘特图

- 数据表：`图14_九个代表性任务甘特图.csv`
- 绘图工具：Origin。
- 图形任务：用少量典型任务展示已保存任务级排程中的执行区间、即时开工、跨区迁移与长等待；不代表全部 50 000 个任务。
- 数据选择：文件按“实时推理—批量推理—AI训练”三类排列，共 9 行真实任务，无空白分组标题行；直接全选导入，不筛选、删除、拼接或计算。9 个任务均从 canonical Cost-primary 的 `q2_schedule_cost_only.csv` 选取，开工时刻位于 2379–2399 h。纵向位置已写入 `Y_绘图位置`（11、10、9；7、6、5；3、2、1），可直接拉开三类任务。
- Origin：导入后选中 `Y_绘图位置`、`X_开始时间（h）`、`X_结束时间（h）`，选择“绘图”→“专用图”→“甘特图”；将 `Y_绘图位置` 作为任务行/纵向位置，“开始”指定为 `X_开始时间（h）`，“结束”指定为 `X_结束时间（h）`。任务名称和标签可调用 `标签_任务ID`、`标签_显示文本`。
- 设置：横轴标题“时刻（h）”，范围 2379–2406；纵轴刻度使用 `Y_绘图位置` 对应的 `标签_任务ID`，隐藏多余数值刻度。实时推理、批量推理、AI训练分别使用蓝、橙、红三色；延期任务 33356、27293 可增加边框或斜线标记。`X_到达时间（h）` 仅作为空心三角参考点或竖线，不能画成甘特条；类别、特征、执行区域和调度状态分别来自 `类别_任务类型`、`类别_特征分支`、`类别_执行区域`、`类别_调度状态`。
- 结论：RT 三条任务均到达即开工，同时包含原区和跨区执行；Batch 展示短延迟、原区即时和终端收尾三种状态；AI训练展示跨区高负载、终端高负载和跨区短延迟。该图仅用于解释调度结构，不作为整体优化效果或全量排程的证据。

## 四、口径与验收

- 图01–图03 的任务柔性统计来自 `Q2_DATA_PROBES.md`；图04–图12和极端等待审计来自最新 canonical 排程与审计 CSV。
- 成本、碳排和收敛图使用 Q2 调度敏感时域 Hour 0–2405；Hour 2406 仅作能源终端结算，不安排计算任务。
- 不把旧 single-pass 结果放入任何图表；不把未统一 Hour-2406 accounting 的绝对跨问比较混入端点图。
- 主结果仍为 `DRAFT / NEEDS_REVIEW`，不宣称全局最优。
