# q1 实时待办

## TODO
- [ ] 完成问题一的数据统计：按 Region×TaskType 统计任务数、GPU需求、Duration、时间窗、来源区域比例与小时到达过程，并整理成正式表/图。
- [ ] 用 ACF、Fano factor、Poisson dispersion/GOF、24 h/168 h 周期检验确认到达过程是否支持平稳/分层 Poisson 假设；不从有限历史中人为制造时序结构。
- [ ] 正式锁定 Q1 预测对象：以“每小时新到达任务的 GPU_Demand 总量”为主要 GPU demand，不把调度后并发 GPU utilization 当成预测目标；GPU-hour 作为工作量补充统计。
- [ ] 建立可解释的历史不足预测模型：优先考察“总到达 Poisson + Region×TaskType 标记分布 + TaskType 条件 GPU mark”的分层标记复合 Poisson；用 Gamma-Poisson / Dirichlet 的部分汇聚处理有限历史下的参数不确定性，并输出预测区间。
- [ ] 按官方时间表执行：0--2351 参数估计，2352--2375 模型选择；模型确定后用 0--2375 重估参数，2376--2399 独立测试。与历史均值、t-24、t-168 等简单基准比较。
- [ ] 明确预测只用于评价；Q1 最后24 h基础调度使用 2376--2399 实际到达任务，不把预测值作为调度输入或向 Q2--Q4 传播。
- [ ] Q1 不建立多目标优化模型、不设置权重。基础算力调度建成规则/状态演化模型：描述任务到达、等待、开工、区域放置、连续执行、资源释放与 2400--2405 drain 的机制。
- [ ] 从附件 baseline 机制出发验证“SourceRegion + ArrivalHour 到达即执行”的已有调度现象；在出现 GPU/IT/Facility/SLA/finish 冲突时按题面合法规则进行必要调整，输出一个满足全部硬约束的基础调度方案。
- [ ] Q1 调度候选域不得人为缩窄：只按 Arrival/EarliestStart、LatestFinish、2406 完成边界、MaxLatency 和单任务自身明确不可能的容量条件剔除候选；不能自行限制最大等待时间、最近区域或固定候选数。
- [ ] 统一按分钟 Duration 与小时重叠长度计算 GPU-hour / AI IT；检查 GPU、IT、Facility、network latency、finish<=2406。2406 禁止任务占用。
- [ ] 生成图：Region×TaskType 任务数/GPU-hour 双热图、三类任务 GPU/Duration 分布、小时任务数与到达GPU需求时间序列、时空柔性散点、0/2351/2375/2399/2400--2405/2406 时间轴、最后24h甘特图与区域GPU利用率。
- [ ] 结果进入正文前登记预测误差、覆盖率/区间宽度、基础调度完成率、SLA、等待/迁移统计、容量峰值至 results/registry.csv。

## NEEDS_REVIEW
- [ ] 复合 Poisson 的 Region×TaskType 分层究竟采用 18 个独立 λ，还是用总 λ + categorical splitting / 分层收缩，需要根据验证误差和参数稳定性比较，不因形式整齐提前冻结。
- [ ] 开工时刻当前按 1 h 粒度解释为整数小时边界；若附件原文支持连续开工，需要同步修改规则调度和 overlap。
- [ ] 基础调度的冲突处理顺序需由题面与 baseline 数据共同确定，不用人为加权目标替代机制；必要时比较不同确定性规则的可行率和结果稳定性。
- [ ] 关键结果进入正文前完成规定检查。

## DONE
- [x] 撤销 `DS-REPTA-B` 旧定义：Q1 不是 SFETA/REPTA 优化问题，而是“需求统计预测 + 基础调度机制建模”。
- [x] 当前路线拒绝把 LSTM 作为默认方法；训练/验证/测试划分只用于统计参数估计、模型选择与独立测试。
- [x] 终极数据审计显示小时任务到达近 Poisson、24 h/168 h 自相关很弱，详见 `shared/Q9_DATA_AUDIT.md`。
- [x] 完成 `GPU_Demand ⟂ SourceRegion | TaskType` 初步检验：AITraining/Batch/RealTime 的 GPU×Region 独立性卡方 p 分别约 0.535/0.776/0.146，Cramér's V 约 0.082/0.032/0.021；区域解释 GPU 方差的 η² 均 <0.001，当前支持 GPU mark 主要按 TaskType 建模。
- [x] 完成 `Duration ⟂ SourceRegion | TaskType` 初步检验：三类任务按区域 Kruskal-Wallis p 约 0.329/0.790/0.142（AITraining/Batch/RealTime），区域 η² 均 <0.0005；当前没有证据需要 Region 条件 Duration 分布。
- [x] 确定 Q1 预测评价与最后24 h实际任务调度解耦。
- [x] 采用 `shared/Q9_SPEC_INTERPRETATION.md` 的时间语义：任务到达止于 2399，2400--2405 为 drain，2406 仅终端结算且禁止任务占用。
