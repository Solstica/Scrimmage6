# q1 实时待办

当前实现真源：`modules/20_q1/code/README.md`。

问题一正式拆成两层：

- **Q1-A：GPU 需求统计 + 24 h 短期概率预测**；
- **Q1-B：最后 24 h 实际任务的基础规则调度**。

Q1-A 的定位是**短期需求统计基线/参考模型**：负责说明已有任务数据中“到达强度—业务组成—资源重量”的结构，并对 2376--2399 h 做一次独立预测评价；不承担长期企业负荷预测，不建立长期趋势、概念漂移或复杂动态状态模型。Q1-B 不使用预测任务，正式调度直接读取 2376--2399 h 实际到达任务。

Q1 不建立多目标优化模型，不设置主观权重，不把电价/碳/新能源作为调度依据。

## PROGRAMMER START HERE

### P0 数据与复现门禁
- [ ] 在 `modules/20_q1/code/q1_data_audit.py`（或等价清晰模块）读取官方数据并复现已有审计：任务数、Region×TaskType、GPU/Duration、小时到达、ACF、Fano、Poisson GOF、条件关系。
- [ ] 生成 `modules/20_q1/data/processed/hourly_arrivals.csv`、`mark_distribution_gpu.csv`、`mark_distribution_duration.csv`；另输出 `(GPU,Duration)|TaskType` 联合经验 mark 数据/频数表，供概率模拟与 GPU-hour 统计使用。
- [ ] 禁止把 2400--2406 的结构性“无新任务”零值加入到达到达率/Poisson 拟合。
- [ ] 在 **0--2351 训练区间内部**仅做模型假设诊断：总流 Poisson、`pi[r,k]` 是否存在明显不稳定、18 个子流 dispersion 是否异常。该诊断不得变成额外模型选择验证集，也不得与 2352--2375 官方验证集混用。
- [ ] 所有统计结果先保持 `DRAFT / NEEDS_REVIEW`，不得直接写入正文。

### P1 Q1-A：24 h 短期预测模型
- [ ] 主模型实现为三层生成结构：`N_t ~ Poisson(lambda)` → `(Region,TaskType) ~ Categorical(pi_rk)` → `(GPU,Duration)|TaskType ~ empirical joint mark`。
- [ ] 主预测量仍为每小时新到达 `GPU_Demand`；其边际 mark 使用 `GPU|TaskType` 经验离散分布。联合 `(GPU,Duration)|TaskType` 用于保留任务资源关系、GPU-hour 工作量与联合模拟，不人为假设 GPU 与 Duration 完全独立。
- [ ] 18 个 Region×TaskType 子流由总 `lambda` 与 `pi_rk` 分裂产生，不默认分别拟合 18 条复杂时间序列。
- [ ] 数学解释必须保留：`E[Y_rkt] = lambda*pi[r,k]*E[G|K=k]`，即“总体到达强度 × 区域-业务组成 × 单任务 GPU 强度”。
- [ ] GPU-hour 辅助量使用联合 mark：`H_rkt = sum(G_i*D_i/60)`，用于解释任务数、GPU demand 与真实计算工作量的差异。
- [ ] 均值/方差优先用复合 Poisson 解析式；完整预测区间可用 FFT/离散递推或固定随机种子的 Monte Carlo。Monte Carlo 只作数值工具，不作为模型主体。
- [ ] 严格按官方切分：0--2351 **只用于参数估计与假设诊断**；2352--2375 **唯一用于模型选择/验证**；模型冻结后用 0--2375 重估；2376--2399 **一次性独立测试**。
- [ ] **禁止**在 0--2351 内再切 rolling/pseudo-backtest 作为额外模型选择依据；避免训练区间与验证逻辑混用。若做训练内部稳定性图，只能作为描述性诊断，不能参与模型选择。
- [ ] 基准只保留必要项：历史均值、t-24、t-168；当前 marked compound Poisson 为主模型。24 h 非齐次 Poisson / NB / quasi-Poisson 只有官方验证集明确显示必要时才启用。
- [ ] 输出 MAE、RMSE、WAPE；若给概率区间，输出 PICP + MPIW/interval score。
- [ ] 所有 Region/TaskType/System 预测必须严格加和一致。
- [ ] 输出 `forecast_validation.csv` 与 `forecast_test_2376_2399.csv`。
- [ ] 不为 Q1 增加长期企业 regime-shift、在线遗忘、DGLM 等扩展；如后续论文评价需要，可一句话说明推广方向，但不占 Q1 主模型篇幅。

### P2 Q1-B：基础调度引擎
- [ ] 实现 `overlap(i,s,t)=max(0,min(t+1,s+p_i)-max(t,s))`，并单元测试 `sum_t overlap = Duration/60`。
- [ ] 复现 baseline：`ExecutionRegion=SourceRegion`、`StartHour=ArrivalHour`，重新计算 AI IT load；应在浮点误差范围复现附件 `Baseline_AI_IT_Load_MW`。
- [ ] Q1-B 只读取 2376--2399 实际到达任务；允许在 2400--2405 继续/结清，2406 禁止任务占用。
- [ ] RealTimeInference：到达即开工；Batch/AITraining：允许等待；所有 Region 必须满足各自 MaxLatency。
- [ ] 统一检查 GPU、IT、Facility、Latency、Earliest/LatestFinish、`finish<=2406`。
- [ ] 不人为缩搜索域：不能自行添加“最多延迟24h”、最近区域、top-K 候选等限制。
- [ ] 冲突处理先做成可插拔确定性规则：baseline、本地 earliest-feasible、必要时枚举全部 SLA 可行 Region；不使用价格/碳/新能源。
- [ ] 输出 `schedule_2376_2405.csv`、`gpu_utilization_2376_2405.csv`、`constraint_audit.csv`。

### P3 图表
- [ ] Region×TaskType 任务数热图。
- [ ] Region×TaskType GPU-hour 热图；与任务数热图并列，用于突出 Training 任务数约 1/3 但 GPU-hour 约 80% 的结构差异。
- [ ] 三类 GPU_Demand 经验分布图，并可叠加离散均匀简化分布作对照。
- [ ] `(GPU,Duration)|TaskType` 联合关系图或紧凑二维统计图；只为说明联合 mark，不额外扩展复杂模型。
- [ ] Duration 全局/分类型分布对照。
- [ ] 小时任务数直方图 + Poisson PMF。
- [ ] ACF 图，突出 lag 1/24/168。
- [ ] 0--2399 到达任务数和到达 GPU demand 时序图。
- [ ] 2376--2399 预测值 + 实际值 + 概率区间。
- [ ] 最后 24 h + drain 的任务甘特图。
- [ ] 六 Region GPU utilization 曲线。
- [ ] 正文图放 `figures/`；可编辑图源放 `figures/editable/`。

### P4 结果登记与交接
- [ ] 在 `tables/` 输出统计表、预测模型比较表、基础调度统计表。
- [ ] 在 `results/registry.csv` 登记关键数值，初始状态统一 `DRAFT / NEEDS_REVIEW`。
- [ ] 提交前给出：运行入口、依赖、随机种子（若有）、输出文件清单、失败/警告、是否通过全部 constraint audit。
- [ ] 不修改 `paper/q1.tex` 的正式数字，等待建模手复核并将结果升级后再写正文。

## 已确定的数据与模型判断

- [x] Q1 不是 SFETA/REPTA 优化问题；它是“统计预测 + 基础调度机制”。
- [x] Q1-A 是 24 h 短期预测基线/参考模型，不承担长期负荷预测；不为 98 天历史引入长期趋势、概念漂移或复杂动态状态。
- [x] 1 h × 0--2399 只有约 2400 个时间观测，不属于高频长历史时序；复杂 LSTM/DGLM/季节模型没有数据依据时不启用。
- [x] 小时总任务到达近 Poisson：均值约 20.8333、Fano≈0.9687，lag 1/24/168 自相关很弱。
- [x] TaskType×SourceRegion 存在明显结构关系（Cramér's V≈0.434），因此 `(Region,TaskType)` 使用联合标记 `pi_rk`，不拆成独立 `P(R)P(K)`。
- [x] `GPU_Demand ⟂ SourceRegion | TaskType` 初步成立：三类 GPU×Region 检验 p 约 0.535/0.776/0.146，区域解释方差 η² < 0.001；GPU mark 主要按 TaskType 建模。
- [x] `Duration ⟂ SourceRegion | TaskType` 初步成立：三类 Kruskal-Wallis p 约 0.329/0.790/0.142，η² < 0.0005；不需要再按 Region 细分 Duration。
- [x] 主任务资源 mark 优先保留 `(GPU,Duration)|TaskType` 联合经验分布；主 GPU demand 使用其 GPU 边际，GPU-hour 使用联合 mark，避免把“相关近 0”误写成严格独立。
- [x] 离散均匀 GPU/Duration 只作简化对照，不作为唯一真实分布假设。
- [x] Gamma-Poisson / Dirichlet Bayesian 更新不作为主模型必需部分；当前样本量下频率估计足够作为主实现。
- [x] 预测对象锁定为“每小时新到达任务的 GPU_Demand 总量”；GPU-hour 是工作量补充，运行中 GPU utilization 由 Q1-B 状态演化得到。
- [x] 官方数据切分必须严格无泄漏：0--2351 参数估计/诊断，2352--2375 模型选择，0--2375 重估，2376--2399 一次性测试；不做跨入训练区间的伪验证选模。
- [x] Q1 最后24 h正式调度使用 2376--2399 实际到达任务，预测误差不进入调度或 Q2--Q4。
- [x] baseline 机制已识别为“SourceRegion + ArrivalHour 到达即执行”，并能重构附件 baseline AI IT load。
- [x] 时间语义：到达止于 2399；2400--2405 为 drain；2406 仅终端能源结算且禁止计算任务占用。

## NEEDS_REVIEW

- [ ] 在 0--2351 训练区间内部只做 `pi[r,k]` 稳定性和 18 子流 dispersion 的**假设诊断**；若没有明显异常，不再扩展模型。该诊断不得用于重新选择模型或调参。
- [ ] 少数 Region×TaskType 子流若出现明显过离散，先判断是否足以影响 24 h 预测区间；若影响很小，保持主模型简洁。只有官方验证集显示区间失真时才局部启用 dispersion/quasi-Poisson。
- [ ] Q1-B 基础调度发生容量冲突后的唯一规则顺序尚未冻结；先由程序手实现确定性可插拔规则并输出对比。
- [ ] 开工时刻当前按 1 h 粒度解释为整数小时边界；若题面后续确认连续开工，需要统一修改 scheduler 与 overlap。
- [ ] 正式正文关键结果只有 `FROZEN + CHECKED` 后才能使用。
