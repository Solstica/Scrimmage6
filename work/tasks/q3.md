# q3 实时待办

当前模型真源：`modules/40_q3/code/Q3_MODEL_SPEC.md`。
数据探针：`modules/40_q3/code/Q3_DATA_PROBES.md`。
批判性审核：`modules/40_q3/code/Q3_CRITICAL_AUDIT.md`。
近十年文献审计：`modules/40_q3/code/Q3_LITERATURE_AUDIT.md`。
小探针：`modules/40_q3/code/Q3_SMALL_PROBES.md`。

## Q3 当前定位

问题三严格固定 `Baseline_AI_IT_Load_MW` 与 `NonAI_IT_Load_MW`，设施负荷按

`L[r,t] = PUE[r] * (Baseline_AI_IT_Load_MW[r,t] + NonAI_IT_Load_MW[r,t])`

计算；不重新优化任务迁移与开工时段。

主模型采用成熟科研问题类型：**fixed-load multi-period BESS optimal/economic dispatch**。不为主模型另造缩写。

当前仍为 `DRAFT / NEEDS_REVIEW`。

## PROGRAMMER / MODELER START HERE

### P0 数据与口径门禁
- [ ] 读取 `region_time_data.xlsx` 0--2406 h 与 `storage_information.xlsx`。
- [ ] 固定 Facility Load，不重新构造任何任务调度。
- [ ] 验证附件能源流恒等式：Renewable allocation / Load balance / Charge decomposition / NetGridImport / Carbon。
- [ ] `InitialSOC_MWh` 是 Hour 0 前唯一初态；附件 `SOC_MWh` 仅为 raw baseline 状态。
- [ ] 记录 RegionE Hour 0 基准 SOC 约 1 MWh 不一致，不修改原数据。
- [ ] 记录附件 baseline D/E/F 的 `SOC(2406)<InitialSOC`；baseline 可对照，但不是 Q3 终端约束下的可行优化点。

### P1 决策变量与平衡
- [ ] 每个 `(r,t)` 建立 `u` 直接新能源、`qR` 新能源充电、`qG` 电网充电、`d` 放电、`gL` 电网供负荷、`s` 新能源售电、`w` 弃电、`E` SOC。
- [ ] `AvailableRenewable = u + qR + s + w`。
- [ ] `Load = u + d + gL`。
- [ ] `GridPurchase = gL + qG`。
- [ ] `ChargePower = qR + qG`。
- [ ] `GridSell=s` 仅允许来源于新能源分配；不得把网购电或储能放电直接转成售电。

### P2 SOC 与硬约束
- [ ] `E[-1]=InitialSOC`。
- [ ] `E[t]=E[t-1]+eta_c*(qR+qG)-d/eta_d`。
- [ ] SOC 上下限；总充电功率上限；放电功率上限。
- [ ] `GridPurchase<=MaxGridImport`。
- [ ] `s<=min(SellLimit,MaxGridExport)`。
- [ ] `E[2406]>=InitialSOC`。
- [ ] Hour 2406 的电力/储能结算计入真实成本和碳，不允许免费补终端 SOC。

### P3 主目标与成本最优面探针
- [ ] Stage 1：最小化 `sum(Price*GridPurchase - SellPrice*GridSell)`。
- [x] 小探针显示主成本最优 + 最小吞吐量解中六区 `PeakImport` 已全部为 0，因此不再把 PeakImport 加为次目标。
- [x] 成本/吞吐量不变条件下，A/B/C/D 已近乎完全平滑；E 的 MeanAbsRamp 仅改善约 `0.0016%`；F 可改善约 `10.21%`，SigmaGrid 可改善约 `4.46%`。
- [ ] 默认保持 `Cost -> Throughput` 两阶段结构；若 Q3/Q4 篇幅允许，可再加可选 Stage 3：`min total absolute NetGridImport ramp`，只作为成本与吞吐量等价解中的代表解选择。
- [ ] 若采用 Stage 3，必须放在 Throughput 之后，避免 A/B/C 因平滑目标的多重最优重新产生无意义储能循环。
- [ ] 不构造 Cost/Carbon/Peak/Fluctuation 的主观加权和，不为显示多目标而人为制造 Pareto 前沿。

### P4 三层对照（强制）
- [ ] `B0`：附件 raw baseline，只作为给定运行状态参考；不得把它当作满足 Q3 终端 SOC 的正式可行优化点。
- [ ] `E0`：同一固定负荷、同一新能源/购售电自由度，但 `qR=qG=d=0` 的 no-storage benchmark。
- [ ] `E1`：完整 BESS economic dispatch。
- [ ] `E1-E0` 才解释为 BESS 增量价值。
- [ ] `B0-E0` 只称“基准运行策略差距”，不得归因于储能。
- [ ] `B0-E1` 只作整体能源策略结果对比，不用于声称储能单独贡献。

### P5 新能源过剩退化处理
- [ ] 不人为制造新能源不足、碳价格或 Cost--Carbon 权重。
- [ ] 明确当前数据中除 RegionF 1 h 外，`AvailableRenewable >= FacilityLoad`；Carbon/PeakImport 可能自然退化。
- [ ] 报告 A/B/C 是否在成本最优下完全不需要 BESS；D/E/F 是否主要由 energy arbitrage / renewable time shifting 驱动。
- [ ] 分区域报告 `RenewableCharge`、`GridCharge`、`Discharge`、售电、弃电变化，解释储能实际承担的服务。

### P6 负荷波动指标修正
- [ ] 题面“负荷波动程度”不能静默改写为 NetGridImport 波动。
- [ ] 因 Q3 Facility Load 固定，先明确报告其优化前后波动变化率为 0。
- [ ] 另加 `NetGridImport` 标准差与必要时平均绝对 ramp，明确称“电网交互功率波动”，用于评价储能对外部电网的平滑作用。

### P7 同时充放电 SCD
- [x] 小探针验证：Stage 1 纯成本 LP 在六区合计出现 2111 个同时充放电 Region-Hour，说明纯成本最优解存在严重多重最优数值循环。
- [x] 在 `Cost<=Cost*+1e-4 CNY` 下最小化吞吐量后，六区 SCD 全部降为 0；总吞吐量由约 `1,331,389.40 MWh` 降至 `346,201.93 MWh`，减少约 `74.00%`；系统总成本仅变化约 `0.000299 CNY`。
- [ ] 正式实现独立复现后，将 `Cost -> Throughput` 固定为主求解规则。
- [ ] 若正式实现仍出现超容差 SCD，优先参考 Applied Energy 2024 的 energy-storage complementarity exact-relaxation / penalty 思路。
- [ ] 只有 exact-relaxation 门禁仍失败时，才升级 MILP 二元互斥约束。
- [ ] 不宣称当前 `Cost -> Throughput` 就是文献 exact-relaxation 定理的原样实现。

### P8 六区域分解
- [ ] 主目标和约束无跨区域能量耦合，严格分解为 6 个独立 Region LP 并行求解，再汇总系统指标。
- [ ] 不添加全系统统一碳预算或系统峰值耦合约束；此类跨区域权衡留给 Q4。

### P9 结果指标
- [ ] Cost：附件统一购电成本减售电收益。
- [ ] Carbon：严格 `GridPurchase * CarbonIntensity`。
- [ ] PeakImport：`max_t max(NetGridImport,0)`。
- [ ] Facility Load fluctuation：固定不变，明确结果为 0 变化。
- [ ] Grid-interaction fluctuation：NetGridImport 标准差；MeanRamp 仅作辅助。
- [ ] Renewable utilization：按附件 Q2--Q4 统一公式重算。
- [ ] 输出终端 SOC、储能总充/放电量、吞吐量、活跃小时数和典型 SOC 曲线。

### P10 时间展开网络流表示（候选）
- [ ] 保留 `time-expanded energy-flow network` 作为同一 LP 的结构化表达候选：每小时建立能源节点，新能源/电网/负荷/售电/弃电为当期流向，SOC 为跨时段储能弧。
- [ ] 当前只改变表示方式，不改变可行域、目标或求解器；是否进入正文取决于 Q4 模型与最终篇幅。
- [ ] 若采用，应优先用于“模型建立示意图/能量流图”，不为了图论形式把当前清晰 LP 改写成更难解释的 generalized-flow 主模型。

## 文献审查后采用的轻量改进

- [x] Energy 2025 数据中心储能研究：支持把 data-center BESS 单独建成 optimal dispatch / electricity-cost minimization，而无需把任务调度重新引入 Q3。
- [x] Applied Energy 2024 exact relaxation：支持“不必一开始就用 MILP 二元变量处理充放电互补”，可优先连续优化并以严格门禁/损失惩罚处理 SCD。
- [x] Journal of Energy Storage 2024 convex dispatch：说明 BESS dispatch 的连续/凸建模是成熟路线；本题不因算法新颖性强行上 MILP。
- [x] Journal of Energy Storage 2025 no-storage scenario：支持用同口径 without-storage benchmark 识别 BESS 集成的增量价值，对应 E0/E1。

## 当前明确不做

- 不重新调度任务；
- 不使用 SFETA/REPTA 任务分配算法；
- 不做 stochastic programming / DRO；
- 不做 MPC；
- 不使用 RL / ADP / GA / PSO；
- 不引入附件没有给出的 battery degradation / SOH / cycle-life cost；
- 不建 shared energy storage / Stackelberg / cooperative game；
- 不建 hybrid hydrogen-battery；
- 不建跨区域线路潮流；
- time-expanded energy-flow network 仅作候选表示，不改变主 LP。

## NEEDS_REVIEW

- [ ] 正式 LP 上复核 RegionE Hour 0 baseline SOC 的约 1 MWh 不一致；原数据不改写。
- [ ] 正式确认 B0 的 D/E/F 终端 SOC 低于 InitialSOC，并在论文中只作为 raw reference。
- [ ] 正式 E0/E1 独立复现 A/B/C 的 BESS 零动作、D/E/F 的储能价值构成，以及 Stage 2 消除 SCD 的结果。
- [ ] 根据 Q4 篇幅决定是否加入时间展开网络流图与可选 Stage 3 平滑代表解。
- [ ] 确认 Hour 2406 的 NonAI 固定负荷与能源结算口径。
- [ ] 关键结果进入正文前完成 registry `FROZEN + CHECKED`。
