# q4 实时待办

当前状态：`DRAFT / NEEDS_REVIEW`

## 真源与最新探针

- 数据/耦合审计：`modules/50_q4/code/Q4_DATA_AND_COUPLING_AUDIT.md`
- Probe 3 exact/Benders/Carbon：`modules/50_q4/code/Q4_PROBE_3_BENDERS_AND_CARBON.md`
- Full explicit Master 可扩展性：`modules/50_q4/code/Q4_FULL_MASTER_SCALABILITY_AND_SCENARIO_PROBE_20260817.md`
- Complete recourse：`modules/50_q4/code/Q4_COMPLETE_RECOURSE_PROBE_20260817.md`
- Benders+CG 实现计划：`modules/50_q4/code/Q4_COLUMN_GENERATION_SCALEUP_PLAN_20260817.md`
- Column Pricing Probe：`modules/50_q4/code/Q4_COLUMN_PRICING_PROBE_20260817.md`
- 数据结构计算压缩：`modules/50_q4/code/Q4_DATA_STRUCTURE_COMPUTE_REDUCTION_PLAN_20260817.md`
- A/B/C 压缩探针：`modules/50_q4/code/Q4_COMPUTE_REDUCTION_ABC_PROBES_20260817.md`
- **完整 Root Benders+CG scaling probe**：`modules/50_q4/code/Q4_ROOT_BENDERS_CG_SCALING_PROBE_20260817.md`
- scaling 复现代码：`modules/50_q4/code/q4_root_benders_cg_scaling_probe.py`
- scaling 数值：`modules/50_q4/results/q4_root_benders_cg_scaling_summary_20260817.{csv,json}`

## 当前正式问题定位

Q4 是 **joint workload--energy--storage scheduling**：

`任务时空决策 x -> AI IT -> Facility Load -> Renewable/BESS/Grid recourse`。

Q2 的任务时空柔性和 Q3 的储能时间柔性会争用同一份时空新能源余量，因此最终模型不能固定 Q2 排程后单向调用 Q3。

完整数学模型保留全部合法 `(task,region,start)` 域；全规模实现使用 **Benders + exact column generation**，不显式展开约 `2.33e8` 个 time-indexed placements。

## DONE — 模型与算法判断

- [x] 撤销 `DS-REPTA-J` / 四问统一 REPTA 旧框架；REPTA 仅保留 sequential baseline 身份。
- [x] 时域统一：0--2399 新任务，2400--2405 drain，2406 仅能源终端结算。
- [x] 网络侧只使用 latency/SLA；不增加潮流、带宽、迁移数据量/能耗/费用。
- [x] Q4 AI IT 必须由 workload + fractional overlap + power_mapping 重算。
- [x] Probe 1：冻结 BESS 的 Q2 排名可制造虚假迁移收益。
- [x] Probe 2：Energy-LP load dual 能有效排序真实 recourse，但只用最新 dual 会出现 A->B->A 振荡。
- [x] Probe 3：40-task full-candidate joint MILP 与真正 Benders 闭合；Cost-only region multi-cut 与 global Carbon cut 均得到 exact benchmark。
- [x] Cost 最优面存在大量服务性能退化解；采用 `Cost/Carbon -> Wait -> Latency` 字典序精化，不用 AHP/人工权重。
- [x] baseline Cost-only complete recourse 已解析证明，因此只需 Benders optimality cuts。

## DONE — 计算规模与结构压缩

- [x] 完整合法域约 `233,375,201` placements；full explicit Master 的 35--47 GB 只是“全部列一次性展开”的表示下界，不等于正式算法实际资源量。
- [x] Facility capacity 与 IT capacity 严格重复，正式 Master 删除 Facility rows。
- [x] GPU/IT 逐 `(region,hour)` exact dominance presolve：40/100/500-task benchmark 资源矩阵 nnz 下降约 58--61%，目标完全不变，LP 时间约减半。
- [x] Benders cut 按 dual support 稀疏装配：40/100/500-task cut payload 比 dense 低约 95%。
- [x] Column Pricing 40-task 对照：完整 3139 columns 与仅 110 active columns 的 CG 得到同一 root bound。
- [x] Pricing 使用 `(TaskType,Region,Duration)` 模板 + suffix-min；flexible task 完整 start domain 不裁剪，RT 固定 start。
- [x] Carbon budget 的 `b_r` 区域预算改写数学等价，但小样本串行实现更慢，因此暂不作为默认 backend。

## DONE — 完整 Root Benders+CG scaling probe

先修复 raw `SourceRegion+ArrivalHour` 中仅 44 个 GPU 超载 Region-Hour；移动 36 个任务得到全 50k GPU/IT 可行参考排程。该修复只用于初始 columns，不限制后续完整合法域。

40-task full-horizon exact 校准：

- full explicit legal columns = `177,995`；
- CG active columns = `44`；
- 两者 root objective 完全一致到 solver tolerance。

Scaling：

| Released tasks | Iter | Active columns | Col/task | Cuts | Internal root time | Peak RSS |
|---:|---:|---:|---:|---:|---:|---:|
| 5,000 | 3 | 5,130 | 1.0260 | 8 | 7.53 s | 631 MB |
| 10,000 | 5 | 10,277 | 1.0277 | 10 | 9.10 s | 631 MB |
| 25,000 | 7 | 25,713 | 1.0285 | 10 | 16.91 s | 641 MB |
| **50,000** | **8** | **51,465** | **1.0293** | **11** | **19.66 s** | **650 MB** |

50k 新进程包含读取附件、重构、可行初始修复和 root 求解的总 wall time 约 `23.62 s`，最大 RSS 约 `649.5 MB`。

**结论修正：** Q4 baseline Cost-only root 已不属于“大内存/数小时”问题。2.33e8 是潜在合法域大小；exact CG 后实际 Root RMP 只约一任务一列。当前 root 中 pricing 每轮约 2.8--3.1 s，RMP LP 约 0.2--0.3 s，六区 Energy LP 约 0.6 s。

## P0 — 正式模型文档

- [ ] 写 `Q4_MODEL_SPEC.md`，逐句映射题面：任务迁移、电价、碳、可用新能源、BESS、区域购售电边界、Latency、任务异构性、六指标、三类场景。
- [ ] 给出完整 joint reference MILP、Benders master/subproblem、region optimality cut 与 full-domain pricing reduced cost。
- [ ] 明确 mathematical full domain 与 implicit full-scale implementation 的区别。

## P1 — 当前最高优先级：整数精化

- [ ] 在 50k root 已生成的 `51,465` columns 上解 **restricted integer master**。
- [ ] 输出 `LB_root / UB_integer / integrality_gap / integer runtime / peak RAM / migrated tasks / waiting / latency`。
- [ ] 对 restricted integer 解重新调用真实 Energy LP，验证所有 Benders recourse 与硬约束。
- [ ] 若 integrality gap 已足够小，则停止，不为算法形式强行实现 branch-and-price。
- [ ] 只有 gap 明显时才进入 branch-and-price / branch-price-and-Benders。
- [ ] Column aging / reduced-cost fixing 只能采用 exact bound；禁止经验 top-k 永久删列。

## P2 — 多目标与服务质量

- [ ] 不使用 AHP/人工加权和。
- [ ] 计算 Cost、Carbon、Latency、QoS、RenewableUtilization、regional PeakImport 单指标锚点与可行范围。
- [ ] Cost/Carbon 主层使用 epsilon-constraint；服务层使用无主观权重字典序精化。
- [ ] QoS 主指标优先 waiting/delay/JCT family；NetworkLatency 单独报告。

## P3 — 场景

- [ ] Carbon：以 cost-opt / carbon-opt 锚点构造 epsilon budget path。
- [ ] Electricity-price mechanism：从附件 PricePeriod/逐时价格出发构造 flat / attachment mechanism / 可解释增强峰谷 sensitivity。
- [ ] Renewable fluctuation：附件 100 日 profile 完全重复，正式只能做透明 scenario construction；重新求 full-system critical threshold，不能沿用局部 `gamma_crit≈1.303326`。
- [ ] 场景 warm-start 复用 baseline root columns/cuts，实测 column growth 与 RAM/time。

## 工程门禁

- [x] baseline Cost-only root 实测远低于目标：约 0.65 GB / 24 s。
- [ ] 最终整数 + 主场景流程目标 peak RAM `<=10 GB`，绝对 `<=20 GB`。
- [ ] 总 wall-clock hard cap `<=9 h`。
- [ ] 任何超过 hard cap 的升级必须先做 exact representation / pool / pricing 优化，不能通过删合法域换速度。

## NEEDS_REVIEW

- [ ] Root scaling 是 LP relaxation，不等于最终整数 Q4。
- [ ] 40-task exact 校准支持 pricing exactness，但还需对最终 50k integer 解做独立 feasibility/recourse audit。
- [ ] 当前 baseline root column 数极少不能外推 Carbon/renewable stress 场景；场景需重新扫描 negative reduced cost。
- [ ] 当前 50k 可行参考排程修复移动 36 个任务，只是初始化，不作为最终策略结论。
- [ ] 2406 纯终端能源动作成本/碳口径继续与附件统一公式保持一致。
- [ ] 关键结果只有 `FROZEN/CHECKED` 后进入论文正文。
