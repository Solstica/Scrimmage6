# q4 实时待办

当前状态：`FINISHED_DRAFT / CANONICAL JOINT ENDPOINT AVAILABLE`

详细最终化任务：`work/tasks/q4_finalization_20260818.md`。

最终结果复核：`modules/50_q4/results/FINAL_RECERTIFICATION_REVIEW_20260818.md`。

最终 summary 真源：`modules/50_q4/results/qos_refinement_multicut/q4_qos_summary.json`。

## Canonical 主问题已完成

`region_multicut_v1` 已完成两轮 final-pool `Cost -> Wait -> Latency` 再认证：

- Cost=`-459340688.8007043 CNY`；
- Total Wait=`0 h`；
- Total Latency=`250860 ms`；
- migrated=`61/50000=0.122%`；
- mean latency=`5.0172 ms`；
- max latency=`58 ms`；
- max finish=`2405.6 h`；
- final active columns=`137158`；
- Benders cuts=`1173`；
- min missing reduced cost=`0`；
- max region Benders violation=`0`；
- hard audit=`PASS`。

Latency full-domain LP lower bound=`250469.954579 ms`，integer representative=`250860 ms`，relative gap≈`0.1555%`。

**禁止写 global integer optimum proved。** `restricted MIP gap=0` 只表示最终活动列池 MIP 已闭合。

## 新的核心机制结论

Q2 Cost-primary 在储能动作固定时：migration≈74.896%，mean wait≈20.475 h；Q4 joint 后：migration=0.122%，Wait=0。

因此当前附件数据下：

> 储能时间柔性替代了绝大部分原本由 workload 时间平移与跨区迁移承担的调节需求；计算柔性只需极少量空间修正即可维持 Cost 最优面内的服务质量。

这不是 Q2 错误，而是 Q2 单侧 workload flexibility 与 Q4 joint flexibility 的角色不同。

## 当前 P0——price_flat 正式场景诊断

详见：`modules/50_q4/code/Q4_PRICE_FLAT_SCENARIO_DIAGNOSIS_REVIEW_20260818.md`。

当前 `price_flat` 已反复实现 LP Benders 闭合、完整域定价闭合和 restricted MIP gap=0，但整数候选经真实 Energy recourse 后仍存在 region theta 低估。当前瓶颈是 integer-Benders / integer-OA closure，不是 CG、内存或 MIP gap。

在继续大量追加 cuts 前，必须先完成：

- [ ] **P0-1 场景数据注入真实性检查**：日志固定输出 scenario attachment 路径、A--F Price mean/std/min/max、flat 场景 `std(Price)=0`、与 canonical 输入差异和文件 hash；当前 flat Cost LP 下界与 canonical Cost 几乎完全相同，必须先排除误用 canonical attachments。
- [ ] **P0-2 canonical 排程跨场景 Cost 证书**：固定 canonical 50k workload schedule，在 price_flat attachments 下重新求真实 Energy/BESS/Grid LP；若整数真实成本 UB 与当前 full-domain Cost LP LB 在 `0.001 CNY` 内相等，则 Cost 层直接认证，无需继续 integer-Benders。
- [ ] 若上述同一 price_flat 成本最优排程仍有 `Wait=0`，则 Wait 理论下界=0，Wait 层同步直接认证，只需继续 Latency。
- [ ] 每个整数候选保存六区 FacilityLoad hash 和真实 region violation，确认是否在探索新退化模式还是重复返回同一模式。

只有 P0 跨场景证书失败时，才进入：

- [ ] **P1 Cost-stage extensive MIP**：扩展现有 `solve_extensive_mip`，允许 `objective=cost, cost_cap=None` 时不建立 cost-cap 行，直接把真实 Renewable/BESS/Grid recourse 并入 Cost 整数模型；先做 100/500/5k 一致性与内存测试，再上 137139 活动列。

当前循环停止门禁：连续15个整数候选不刷新 best violation、继续60--90 min无 `stage_completed`、出现重复负荷 hash 或 cuts 快增但 best violation 不再下降，即保留 checkpoint 后切换上述方案。

禁止为场景求解放宽 `0.001 CNY`、裁剪合法域、缩短开工窗口、改变 Cost->Wait->Latency 字典序或临时改变 SellPrice 语义。

## 当前 P0——不再改 canonical 主算法

### 1. Final artifacts 入库
- [ ] 最终 `q4_recertification_stage_metrics.csv` 入库；
- [ ] 50k 最终任务排程 CSV 入库；
- [x] `q4_qos_summary.json` 已切换到 FINISHED_DRAFT final summary；
- [x] final result review 已入库。

### 2. 正式场景
- [ ] Carbon constraints；
- [ ] electricity-price mechanisms；
- [ ] renewable fluctuation scenarios；
- [ ] 每个场景都重新 joint optimize workload + BESS/Grid + exact pricing；
- [ ] `gamma=1.4` 仍只作 diagnostic。

### 3. 六指标
- [ ] canonical + 每个正式场景统一输出 Cost / Carbon / Wait-QoS / Latency / RenewableUtilization / regional PeakNetImport；
- [ ] 不做六指标加权综合分。

### 4. 跨问统一
- [ ] Q2/Q3/Q4 accounting 对齐；
- [ ] 统一 Hour 2406 terminal settlement；
- [ ] 统一 Cost/Carbon/RenewableUtilization/PeakNetImport；
- [ ] accounting 完成后冻结 `Q2-only vs Q3-only vs Q4-joint` 比较。

### 5. 论文与图
- [x] `q4.tex` 已加入 50k final-pool recertification 和柔性替代关系；
- [x] 图审已切换到 final result 状态
- [x] 插入 q4_flow.png 流程图（替换原 TikZ 流程图）
- [x] 编译验证：Q4 部分 PDF 成功生成（651KB @ 2 pages）
- [ ] 区域优势错位结构矩阵；
- [ ] 顺序 Q2->Q3 失真 probe；
- [ ] final recertification 证据表/次级图；
- [ ] Q2/Q3/Q4 unified comparison；
- [ ] 三类正式场景图。

## 整问冻结门禁

只有以下全部完成，Q4 才能从 `FINISHED_DRAFT` 升级到 `FROZEN + CHECKED`：

- [x] canonical final-pool recertification；
- [x] hard audit PASS；
- [ ] final schedule / stage metrics 正式入库；
- [ ] canonical 六指标齐全；
- [ ] Carbon constraints；
- [ ] price mechanism；
- [ ] renewable fluctuation；
- [ ] Q2/Q3/Q4 accounting 统一；
- [ ] 正文、图、表和 registry 同一真源。

## 禁止事项

- 不再继续给 canonical 主算法加 branch-and-price / stabilization / 新启发式；
- 不恢复旧 restricted-pool `378199 h / 1171 h` 结果；
- 不把 `MIP gap=0` 写成 full-domain global integer optimum；
- 不把 Q3/Q4 未统一 accounting 的成本差写成正式 synergy；
- 不为正式场景反向修改 canonical 模型。
