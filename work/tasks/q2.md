# q2 实时待办

当前数据探针真源：`modules/30_q2/code/Q2_DATA_PROBES.md`。

## TODO
- [ ] 以 Q1 的任务统计规律、基础规则调度与硬约束解释为前提，正式进入能源感知的任务时空分配；算法候选名暂定 `SFETA`（Spatio-temporal Flexibility and Energy-aware Task Assignment）。
- [ ] 以 2026 Applied Energy 的 REPTA 为来源算法和对照：保留其非迭代、领域规则驱动、时空任务分配思想，但不照搬其具体能源规则。
- [ ] 对每个任务构造完整合法时空域 `C_i = R_i × T_i`：由 EarliestStart、LatestFinish、finish<=2406、MaxLatency 等硬条件决定；不得为提速人为限制最大延迟、最近区域、候选数量。
- [ ] 用 network_latency + 20/80/150 ms 门限先做硬 SLA gate；不建立题目未给出的带宽、迁移数据量、传输能耗或迁移费用。
- [ ] SFETA 任务排序优先采用由数据直接支持的柔性字典序：RT/低空间柔性/低 slack 优先，再考虑 GPU-hour 资源重量；不使用人为加权柔性 Score。
- [ ] 正式核对 Q2 能源层口径：题面显式决策变量只有任务迁移与开工时段，`storage_information.xlsx` 主要对应 Q3/Q4；因此 Q2 不允许自由重优化储能。需进一步确认 region_time_data 中哪些基准能源状态固定、任务迁移后哪些量做边际重算。
- [ ] 能源感知优先围绕 `Curtailment_MW` / 基准能源余量构造：候选增量负荷先吸收实际弃电，弃电耗尽后才进入购电边际；BuyPrice 与 CarbonIntensity 在购电边际状态下继续区分候选。
- [ ] 暂不冻结 SellPrice 三状态机会成本。是否将 GridSell/SellPrice 纳入 Q2 运行成本，以及是否可使用 storage_information 的 MaxGridExport/SellLimit，必须先按题意确认，不能把 Q3/Q4 的购售电边界偷渡到 Q2。
- [ ] 在上述正确能源口径下重新做 Cost/Carbon 极值探针：先确认是否仍有 `min Carbon = 0`、最低成本是否同时零碳，再决定 lexicographic / epsilon-constraint / 单目标+评价指标；不使用 AHP/TOPSIS 或任意加权和。
- [ ] 严格执行 Q2 时间边界：0--2399 产生实际任务；2400--2405 只结清此前到达弹性任务并计入能源/成本/碳；任务不得占用 2406。
- [ ] Q2 调度后所有 AI IT、Facility Load、CarbonEmission、Renewable Utilization 等需要由实际任务和统一功率映射按最终能源口径重算；不得直接继承 baseline 结果列作为优化结果。
- [ ] 以原始 REPTA 规则作为算法 baseline，并设计 SFETA 消融：去掉任务柔性排序、只按能源状态、不使用碳信号等，比较结果与计算时间。
- [ ] 生成图：任务类型感知区域迁移网络、逐时 Curtailment/Carbon/Price 关系、Q1→Q2 负荷迁移图、边际能源状态示意、REPTA 与 SFETA 算法流程对照。

## NEEDS_REVIEW
- [ ] SFETA 为当前候选名；正式论文前再次检查 acronym collision 和命名必要性。FETA 已在其他 task-assignment 文献中被使用，因此当前不采用 FETA。
- [ ] Q2 能源层“固定基准背景 + 边际任务负荷”的精确守恒关系尚未冻结，这是当前最重要的解释问题。
- [ ] 若正式口径允许 SellPrice/GridSell，需重新定义边际成本；若不允许，则 Q2 只使用 Curtailment→GridPurchase 两状态能源边际。
- [ ] 多目标最终如何组织必须根据正确能源口径下的真实 Cost/Carbon 冲突程度决定，不为了生成 Pareto 冲突而人为加权。
- [ ] 开工时刻当前探针按 1 h 粒度解释为整数小时；正式模型仍需保持这一假设可审查。
- [ ] 关键结果进入正文前完成规定检查。

## DONE
- [x] 撤销 `DS-REPTA-C` / “四问统一 REPTA mode”旧表述；REPTA/SFETA 从 Q2 才正式进入。
- [x] 已读取 REPTA 全文并确认：REPTA 是三阶段策略的 Stage II；Phase A 消纳 local RES overproduction，Phase B 跨区找 RES overproduction，仍无则按最低电价分配；详见 `shared/Q9_SFETA_ADAPTATION.md`。
- [x] 确定 Q2 不自行预测任务、电价、碳强度和新能源，使用实际到达任务与实际逐时电力参数。
- [x] 采用 `shared/Q9_SPEC_INTERPRETATION.md`：2400--2405 为 drain、2406 禁止任务占用；网络侧不补充题目排除的迁移/带宽模型。
- [x] 完成“完全重分配 AvailableRenewable”退化探针：在当前整数小时解释下可构造 50000/50000 任务全可行、52 次迁移、0 延迟、0 购电、0 碳排的排程。该结果说明这种能源解释会使 Q2 碳目标退化，因此不作为正式口径。
- [x] 完成基准能源状态探针：0--2399 共 14400 个 region-hour 全部存在 Curtailment，13230 个同时存在 `Curtailment>0` 与 `GridPurchase-GridCharge>0`；累计弃电约 7.748 TWh，为基准 AI 设施侧能量约 8.15 倍，说明任务迁移具有显著“移出电网供能、移入弃电吸收”的边际空间。
- [x] 发现 `AvailableRenewable_MW` 六区域逐小时严格相同，但 Curtailment 存在显著空间差异：每小时六区弃电极差中位数约 443.30 MW；RegionA 2394/2400 h 为弃电最高，RegionE/F 几乎始终最低。因此 Q2 能源感知必须使用实际能源状态而不是 E/F 区域标签。
- [x] 发现 Curtailment 与 CarbonIntensity 在六区域均显著负相关（约 -0.69 至 -0.85），故优先吸收弃电本身天然带有低碳含义，不需要人为设置新能源/碳权重。
- [x] 购电边际信号中，PUE 调整后的最低 BuyPrice 与最低 CarbonIntensity 在 2400/2400 h 都为 RegionE；成本与碳的空间冲突较弱，需在正式边际能源口径下再做极值探针。
- [x] 完成完整合法域规模探针：RT 总候选 48,050；Batch 114,547,455；Training 118,779,696；合计约 233,375,201。RT start 固定且空间受限，Batch/Training median slack 均约 1200 h，Training GPU-hour 中位数约 194.35，任务柔性差异足以支撑 least-flexible-first 的结构化 SFETA。
