# q2 实时待办

## TODO
- [ ] 以 Q1 的任务统计规律、基础规则调度与硬约束解释为前提，正式进入能源感知的任务时空分配；算法候选名暂定 `SFETA`（Spatio-temporal Flexibility and Energy-aware Task Assignment）。
- [ ] 以 2026 Applied Energy 的 REPTA 为来源算法和对照：保留其非迭代、领域规则驱动、时空任务分配思想，但不照搬“local RES -> remote RES -> lowest price”的具体规则。
- [ ] 对每个任务构造完整合法时空域 `C_i = R_i × T_i`：由 EarliestStart、LatestFinish、finish<=2406、MaxLatency 等硬条件决定；不得为提速人为限制最大延迟、最近区域、候选数量。
- [ ] 用 network_latency + 20/80/150 ms 门限先做硬 SLA gate；不建立题目未给出的带宽、迁移数据量、传输能耗或迁移费用。
- [ ] 将任务级柔性显式化：时间可选域、空间可选域、剩余 slack、GPU×Duration 等用于确定任务有多难安排；优先设计确定性排序规则，不用任意权重和黑箱元启发式。
- [ ] 将原 REPTA 的 RES overproduction/最低购电价规则改为本题能源边际机会状态：消纳弃电约为 0；挤占可售新能源约为 SellPrice；新增购电为 BuyPrice。候选分配后动态更新相应能源余量。
- [ ] 明确 `AvailableRenewable_MW` 六区域逐时相同这一数据特征会削弱原 REPTA 的跨区域 RES complementarity，因此空间选择主要由区域经济/碳偏序、售电机会成本、SLA 和容量共同决定。
- [ ] 检查“新能源富余导致购电碳目标退化到0”的正式可行性；若成立，不通过调权重制造碳冲突，而如实分析售电机会成本、时限、峰值和新能源利用率。
- [ ] 严格执行 Q2 时间边界：0--2399 产生实际任务；2400--2405 只结清此前到达弹性任务并计入能源/成本/碳；任务不得占用 2406。
- [ ] Q2 优化后所有 AI IT、Facility Load、UsedRenewable、GridPurchase/GridSell、CarbonEmission、Renewable Utilization 均从实际任务和统一功率映射重新计算。
- [ ] 以原始 REPTA 规则作为算法 baseline，并设计 SFETA 消融：去掉任务柔性排序、去掉 SellPrice opportunity cost、只保留原 REPTA 规则等，比较结果与计算时间。
- [ ] 生成图：任务类型感知区域迁移网络、电价-碳强度-新能源曲线、Q1→Q2 负荷迁移图、边际机会成本三状态示意、REPTA 与 SFETA 算法流程对照。

## NEEDS_REVIEW
- [ ] SFETA 为当前候选名；正式论文前再次检查 acronym collision 和命名必要性。FETA 已在其他 task-assignment 文献中被使用，因此当前不采用 FETA。
- [ ] SFETA 任务优先级的确切确定性规则需在 Q1 条件分布与 Q2 实际容量实验后冻结，避免先造公式再逼数据适配。
- [ ] 多目标如何进入最终评价需按题目问题二原文处理；目前不使用 AHP/TOPSIS 或人为加权和替代物理量。
- [ ] 2406 的纯终端能源结算是否计入最终电费/碳排需按附件 1 原始公式确认。
- [ ] 关键结果进入正文前完成规定检查。

## DONE
- [x] 撤销 `DS-REPTA-C` / “四问统一 REPTA mode”旧表述；REPTA/SFETA 从 Q2 才正式进入。
- [x] 已读取 REPTA 全文并确认：REPTA 是三阶段策略的 Stage II；Phase A 消纳 local RES overproduction，Phase B 跨区找 RES overproduction，仍无则按最低电价分配；详见 `shared/Q9_SFETA_ADAPTATION.md`。
- [x] 确定 Q2 不自行预测任务、电价、碳强度和新能源，使用实际到达任务与实际逐时电力参数。
- [x] 采用 `shared/Q9_SPEC_INTERPRETATION.md`：2400--2405 为 drain、2406 禁止任务占用；网络侧不补充题目排除的迁移/带宽模型。
